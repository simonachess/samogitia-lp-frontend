# Example environment variables (do NOT commit real secrets)
# Copy this to `.env.local` for local dev. For production, set these in your host (Vercel, etc.).

# ---- Public (exposed to client) ----
# Site URL used for canonical, sitemap, OpenGraph (production: https://samogitiagroup.lt)
NEXT_PUBLIC_SITE_URL=https://samogitiagroup.lt

# Sanity CMS
NEXT_PUBLIC_SANITY_PROJECT_ID=your_sanity_project_id

# Optional: Google Analytics (only loads after cookie consent)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ---- Private (server-only) ----
# Contact form (Resend)
RESEND_API_KEY=re_xxxxxxxxxxxx
CONTACT_TO_EMAIL=samogitiagroup@gmail.com

# Sanity (if you need write from server)
# SANITY_API_TOKEN=your_sanity_api_token
